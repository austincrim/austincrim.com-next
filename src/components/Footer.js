const Footer = (props) => (
    <footer className="px-10 py-8 mx-auto text-sm font-light tracking-wide text-center text-gray-600 dark:text-gray-100">
        {props.children}
    </footer>
);

export default Footer;
